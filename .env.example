# Google Maps Platform（前端專用）
# 用途：① 招生熱點圖 ② 娃娃車即時監看 /bus/monitor 的底圖
#   兩者都是「有設就用 Google、沒設就用 Leaflet + OpenStreetMap」，未設不影響功能。
# 計費：每建立一次地圖實例算一次 Dynamic Maps load，每月前 10,000 次免費、
#   超出 $7/1,000。監看頁只在班次行駛中才建實例，且位置更新只移動 marker 不重建。
#   啟用前請在 Google Cloud Console 設每日 quota 上限，避免非預期爆量。
# 注意：VITE_ 變數會暴露在瀏覽器端，請使用「前端專用」API key
# 並在 Google Cloud Console 設定 HTTP referrer 限制
# 例如：
#   http://localhost:5173/*
#   http://127.0.0.1:5173/*
#   https://your-domain.example.com/*
VITE_GOOGLE_MAPS_API_KEY=

# 若未設定，招生熱點圖會維持 Leaflet + OpenStreetMap fallback
# VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
# VITE_MAP_ATTRIBUTION=&copy; OpenStreetMap contributors

# 教師端 LINE Bot 加好友連結
# ⚠ 已改由後台「LINE 設定」提供（多租戶 4d/fb：每間園所一組官方帳號，
#   烤進 bundle 的值在多租戶下必然錯）。此變數僅為過渡期 fallback，
#   預設留空；階段 3 連同 Dockerfile ARG 一起刪除。
VITE_LINE_BOT_FRIEND_URL=

# ── 多租戶（4d）──────────────────────────────────────────────────────────
# 兩者皆空 = 單租戶模式，全前端行為與改造前逐字相同（灰度不變式 DEV-05）。
# VITE_TENANT_BASE_DOMAIN：subdomain 樣板的 base domain（例：ivy.tw →
#   yihua.ivy.tw 解析出 slug=yihua）。本機模擬可用 localhost（yihua.localhost）。
# VITE_TENANT_DOMAIN_MAP：既有正式網域 → slug 的 JSON 對照，承接自訂網域租戶。
# VITE_TENANT_META_ENABLED：品牌 API 灰度開關。留空 = 跟隨上面兩個變數；
#   1 = 單租戶也啟用；0 = kill switch。後端 tenant-meta 上線後應把它整個刪掉。
# VITE_TENANT_BASE_DOMAIN=
# VITE_TENANT_DOMAIN_MAP={"ivypreschool.tw":"yihua"}
# VITE_TENANT_META_ENABLED=

# Sentry 錯誤監控（DSN 留空 = no-op）
# VITE_SENTRY_DSN=
# VITE_SENTRY_ENVIRONMENT=production
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# build-time sourcemap 上傳（只 CI 設）
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=ivy-frontend
